import React from "react";
import FontAwesome from "react-fontawesome";

const Marker = props => (
  <div>
    <FontAwesome name='map-marker' size='3x' style={{ color: "#FF0000" }} />
  </div>
);

export default Marker;
